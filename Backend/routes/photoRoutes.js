// const express = require('express');
// const { creatImage, updateImage, getAllImages, getSingleImage, deleteImage }
//  = require('../controllers/ImageController');
// const router = express.Router()
// //const admin = require('../middleware/admin')

// router.get('/', getAllImages);
// router.get('/:imageId', getSingleImage);
// // here to avoid to put them in one file 
// router.post('/', creatImage);
// router.put('/:imageId', updateImage);
// router.delete('/:imaageId', deleteImage);

// module.exports = router;